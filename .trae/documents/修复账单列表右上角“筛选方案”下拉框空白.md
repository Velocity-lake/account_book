## 我的理解
- 你要删除账单列表右上角的“筛选方案”功能，包括下拉框和相关按钮，不再支持保存/管理/应用筛选预设。
- 其它筛选（年份、月份、金额、搜索等）保留；“显示列管理”的基础/扩展视图与此无关，继续保留。

## 变更范围
- 移除 UI 控件与按钮：`self.cb_preset`、`应用方案`、`保存方案`、`管理方案`（`c:\Users\gang.luo\Documents\trae_projects\accout_book\ui_bill_list.py:74-79`）。
- 删除相关方法：
  - `_load_presets`（`ui_bill_list.py:203-210`）
  - `save_preset`（`ui_bill_list.py:211-233`）
  - `apply_preset`（`ui_bill_list.py:235-266`）
  - `open_preset_manager`（`ui_bill_list.py:267-288`）
  - `_preset_rename` 与 `_preset_delete`（`ui_bill_list.py:289-345`）。
- 清理引用：按钮的 `command` 引用上述方法，一并移除；其余代码未再引用这些方法（已检索确认）。
- 数据兼容：不改动 `storage.default_state`；历史 `prefs.bill_list.presets` 若存在，原样留在 `ledger.json`，只是 UI 不再使用，避免破坏已有数据。

## 验证
- 启动应用，确认工具栏右侧不再显示该下拉框与三个按钮，布局正常。
- 常规筛选、重置、查重、导入等功能均可正常使用。
- 搜索代码确认不再存在对 `cb_preset` 或上述方法的引用（避免运行期错误）。

## 回滚
- 若需恢复此功能，可从版本控制或备份中恢复删除的代码块；数据仍在 `prefs.bill_list.presets` 中，无需迁移。

请确认是否按上述方案执行。我得到你的同意后开始修改并验证。