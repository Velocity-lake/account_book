## 我的理解
- 点击趋势图数据点没有打开账单列表，是因为点击事件绑定写成了 `"Button-1"`（缺少尖括号），正确写法应为 `"<Button-1>"`。因此根本没有触发 `on_trend_click` 回调。
- 位置在 `ui_dashboard.py` 的 `build_ui` 中给趋势图绑定点击事件的那一行。

## 修改方案
- 将 `self.canvas_trend.bind("Button-1", self.on_trend_click)` 改为 `self.canvas_trend.bind("<Button-1>", self.on_trend_click)`。
- 保留之前已修复的坐标转换（`canvasx/canvasy`）和命中半径逻辑。

## 验证
- 冷启动后，在趋势图上点击某一数据点，应弹出账单列表筛选视图（按日或按月）。

请确认后我立即修改并验证。