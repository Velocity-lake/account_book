## 目标
- 所有位置的“新增记账”统一使用 `AddTransactionDialog`。
- “消费类别”采用下拉可选+可输入的组合（保留可输入能力），在点击“确定”后将新输入的类别自动加入类别库。

## 现状
- 统一模块：账单列表、首页总览、账户管理均已调用同一模块 `AddTransactionDialog`。
- 类别输入：`AddTransactionDialog` 当前为可输入的 `ttk.Combobox(state="normal")`，并会根据“所属类别”动态加载已有类别列表。
- 新类别入库：各入口在保存时均已调用 `add_category(...)` 将新输入的类别加入库：
  - 首页总览：`ui_dashboard.py::_record_for_day` 添加类别
  - 账单列表：`ui_bill_list.py::on_record_for_row` 添加类别
  - 账户管理：`ui_account_manager.py::on_add_transaction_for_account` 添加类别

## 调整与确认
- 保持 `AddTransactionDialog` 的类别输入为可输入+可选（不改为只读）。
- 确认上述三个入口均在保存时执行类别入库逻辑，保持一致行为；若后续新增入口，同样复用该模式。

## 验证
- 在三个入口分别打开新增记账：
  - 下拉中可选择已有类别，也可输入新类别文本。
  - 点击“确定”后，新类别自动加入库，并可在后续下拉中选择。

若你同意此方案，我将进行一次端到端验证，确保三处入口行为一致，无需进一步代码变更。