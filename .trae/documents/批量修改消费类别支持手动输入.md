## 现状确认
- 列表页与仪表盘均已提供“或手动输入”的 `Entry`，但“消费类别”下拉框设置为只读，无法直接在下拉框内键入新类别。

## 改动内容
- 将批量修改对话框中的“消费类别” `ttk.Combobox` 改为可输入（`state="normal"`），允许直接键入新类别。
- 保留“或手动输入” `Entry`：
  - 点击“确定”时，优先取手动输入框内容；若为空，则取下拉框当前值（无论是选择还是键入）。
- 将最终类别通过 `add_category(state, scene, cat_final)` 写入常用类别列表，确保下次下拉可见。

## 影响范围
- 列表页：`ui_bill_list.py` 的 `bulk_modify_category` 对话框。
- 仪表盘：`ui_dashboard.py` 的 `_bulk_modify_category` 对话框。

## 交互细节
- 初始“所属类别”默认为“支出”，切换“收入/支出”时刷新下拉候选；下拉支持键入，不限制为候选列表。
- 标签文案保持“或手动输入”，两种输入方式均可；输入优先级：手动输入 > 下拉框。

## 验证
- 选择若干记录，键入一个下拉中不存在的新类别并确定：记录类别更新成功，且新类别被添加到对应场景的常用列表，下次打开可见。
- 对“收入”“支出”分别验证；无选中记录时不弹窗；取消操作不更改数据。

## 兼容性
- 不改动数据结构与存储逻辑；仅调整控件状态与取值顺序，风险极低。